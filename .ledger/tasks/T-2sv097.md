---
id: T-2sv097
title: Protocol and contract pass: trailer paragraph collision, claim/brief discoverability, bounded search, no self-answered HUMAN questions, ok/errors semantics, usage-error envelope, info_count, list --blocked-on
status: in_progress
priority: p2
size: s
created: 2026-09-02T11:44:58Z
claimed_by: claude-2026-09-01-b
claimed_at: 2026-09-02T12:17:17Z
tags: protocol, ergonomics
---

## Spec

### Findings (sweep 2026-09-02, ergonomics #4, #9, #10, #11, #12, #13; docs #4)

- Protocol: the trailer must be in the SAME final paragraph as Co-Authored-By / Claude-Session lines (a blank line before them drops it; 2 of 47 commits in this repo have that shape and survived only via the bookkeeping channel); name `ledger claim <id>` (a human named the task) and `ledger brief <id>` (resuming one you hold) in step 2; bound the mandated search (`--in spec,log -n 5`); "Never answer your own HUMAN: question"; `ok` is the success signal, `errors` also carries warning/info rows and only errors carry fix_hints; "fix every ERROR and WARNING you caused". Stay under the size pin — replace, do not append.
- `Parser.error` emits the JSON envelope (usage, exit 3) when `--json` is among the args; `validate` adds `info_count` (warning_count stays warnings+info for one version — document).
- `list --blocked-on PREFIX` (substring on the header value); the protocol's integrator queue line uses it.
- README: a table of CLI-only refusal codes (code, exit, when) — none is documented today.
- Retarget hints that send agents to `show` for step/question indexes to `brief`.
Tests for the envelope, the filter, and the protocol phrases.

## Next Steps

## Open Questions

## Commits

## Log

- 2026-09-02T11:44:58Z [claude-2026-09-01-b] add: created: Protocol and contract pass: trailer paragraph collision, claim/brief discoverability, bounded search, no self-answered HUMAN questions, ok/errors semantics, usage-error envelope, info_count, list --blocked-on [p2/s] (tags: protocol, ergonomics)
- 2026-09-02T12:17:17Z [claude-2026-09-01-b] claim: claimed
